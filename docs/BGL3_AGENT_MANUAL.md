# الموسوعة التقنية الشاملة والعميقة لوكيل BGL3 الذكي (The BGL3 Agent Technical Mega-Encyclopedia)

هذا المستند هو المرجع الهندسي النهائي، وصُمم ليكون دليلاً معمارياً شاملاً يغطي كل تفاصيل الوكيل من "الحركة الفيزيائية" للماوس إلى "الاستدلال الاستراتيجي" المعقد، وصولاً إلى الحوكمة الصارمة والتعلم المستمر.

---

## 🏛️ 1. الجهاز العصبي المركزي (AgencyCore) 🧠

* **الموقع الهيكلي**: يمثل الكلاس `AgencyCore` في `agency_core.py` قمة الهرم الإداري والمنظم الأساسي لكل دورات الحياة (Lifecycles).
* **الوظائف الجوهرية**:
  * إدارة الـ Orchestration بين المستشعرات (Sensors) والمنفذات (Actuators).
  * القيام بعملية الـ `Full Diagnostic` التي تجمع البيانات من كل الطبقات.
  * إدارة نظام التصاريح (Permissions) لضمان أن الوكيل لا يتصرف إلا ضمن الصلاحيات الممنوحة.
* **سير العمل (Lifecycle Flow)**:
    1. استقبال الأمر من المستخدم أو مؤقت التشغيل.
    2. تهيئة كافة الـ Engines (Inference, Safety, Indexer).
    3. فحص موارد الجهاز عبر `BrowserCore`.
    4. توزيع المهام على الـ Orchestrator.
* **نمط التصميم**: يستخدم نمط الـ `Singleton` لضمان وجود "رؤية موحدة" لنظام الوكيل.

---

## 🧠 2. محرك الاستدلال التحليلي (Inference Engine) 🧐

* **الموقع الهيكلي**: `inference.py` و `inference_patterns.json`.
* **الوظائف الجوهرية**:
  * استنباط الأنماط (Pattern Deduction) عبر تحليل قاعدة المعرفة (`knowledge.db`).
  * اكتشاف "تجمعات الفشل" (Failure Clusters) وتصنيفها برمجياً.
  * اقتراح "خطة عمل" (Fix Proposal) بناءً على الخبرات السابقة.
* **سير العمل (Lifecycle Flow)**:
    1. سحب بيانات الـ `Runtime Events` الأخيرة.
    2. تطبيق خوارزمية التجميع (Clustering) لتحديد الأنماط.
    3. استدعاء الـ LLM لترجمة النمط إلى "اقتراح معمارى".
    4. تخزين المقترح في `proposed_patterns.json`.
* **تحويل البيانات**: يقوم المحرك بتحويل الأحداث الخام (Raw Events) إلى معلومات استراتيجية (Strategic Insights).

---

## 🤖 3. تكامل Llama 3.1 & Ollama (The Strategic Brain) 🧠

* **التوصيف التقني**: دمج نموذج **Llama 3.1 (8B)** عبر منصة **Ollama** المحلية على المسار `http://localhost:11434`.
* **المميزات الاستراتيجية**:
  * **الخصوصية (Privacy)**: كل عمليات الربط والتحليل تتم محلياً 100% دون إرسال كود المشروع للخارج.
  * **الذكاء الهجين (Hybrid Intelligence)**: يستخدم الـ LLM للتشخيص العالي المستوى بينما يترك التنفيذ للكود المحدد.
  * **التفاعل المباشر (CLI Interaction)**: يمكنك التحدث مع "عقل الوكيل" مباشرة عبر سطر الأوامر باستخدام:

      ```bash
      python scripts/ask_ai.py "سؤالك البرمجي هنا"
      ```

* **سير العمل (Lifecycle Flow)**:
    1. استلام "السياق البرمجي" (Context) مرفقاً بالمشكلة.
    2. إرسال Prompt مهيكل للنموذج المحلي.
    3. استقبال الرد وتحليله برمجياً لفلترة أي "هلوسة".
    4. تحويل النص لخطوات تنفيذية في `Patcher`.

---

## 📂 4. المفهرس الشجري المعمق (Entity Indexer - AST) 📂

* **التقنية الأساسية**: يستخدم `EntityIndexer` في `indexer.py` بالتعاون مع جسر PHP (`ast_bridge.php`).
* **القدرات التحليلية**:
  * فهرسة الكلاسات، الدوال (Methods)، والتبعيات (Dependencies).
  * فهم الـ Namespaces والتداخلات بين الحزم (Packages).
* **سير العمل (Lifecycle Flow)**:
    1. مسح الملفات المعدلة زمنياً (Modified Files).
    2. إرسال المسارات لـ `ast_bridge.php`.
    3. استلام الـ JSON المعبر عن شجرة الرموز (Symbols Tree).
    4. تحديث جداول `entities` و `methods` في الذاكرة.
* **الأثر**: يسمح للوكيل بإدراك "بنية الكود" كما يراها المفسر (Compiler).

---

## 🗺️ 5. مفهرس المسارات الحركي (Route Indexer) 🗺️

* **المكون التقني**: `route_indexer.py`.
* **الوظائف الجوهرية**:
  * مسح ملفات توجيه المسارات في Laravel (`routes/*.php`).
  * ربط كلي بين الروابط الظاهرية (URIs) وبين المتحكمات (Controllers).
* **سير العمل (Lifecycle Flow)**:
    1. استخدام Regex لمسح تعريفات الـ Routes.
    2. استخراج "اسم المتحكم" و "الدالة المستهدفة".
    3. مطابقة الملفات الفيزيائية على القرص.
    4. تسجيل الخريطة النهائية في جدول `routes`.

---

## 💾 6. الذاكرة الهيكلية الشاملة (Structure Memory) 💾

* **قاعدة البيانات**: `knowledge.db` المستندة إلى `SQLite`.
* **الوضعية التشغيلية**:
  * تفعيل `PRAGMA journal_mode=WAL` للسرعة.
  * استخدام `Synchronous=OFF` في بيئات الاختبار.
* **سير العمل (Lifecycle Flow)**:
    1. فتح اتصال (`Connection`) قصير المدى.
    2. تنفيذ العمليات (Transactions) لضمان اتساق البيانات.
    3. إغلاق الاتصال لتحرير القفل (Locks).
* **المحتويات**: تخزن الجدوال الرئيسية التي تمثل "خارطة الجينات" للمشروع.

---

## 🔋 7. الذاكرة الخبرية وتلخيص السياق (Experiential Memory) 🔋

* **المحرك التقني**: `context_digest.py`.
* **الوظيفة الاستراتيجية**:
  * تحويل "تيار الوعي" للوكيل (ملايين الأحداث) إلى "خبرات مركزة" (`Experiences`).
* **سير العمل (Lifecycle Flow)**:
    1. قراءة جدول `runtime_events`.
    2. البحث عن الأفعال التي أدت إلى "نجاح" أو "فشل".
    3. إنشاء ملخص نصي مع درجة ثقة (Confidence).
    4. إدراج الخبرة في جدول `experiences`.

---

## 🔍 8. نظام رصد وتحديد الفشل (Fault Locator) 🔍

* **المكون التقني**: `fault_locator.py`.
* **سير العمل (Lifecycle Flow)**:
    1. رصد الرابط (URL) في المتصفح.
    2. استرجاع الكود المربوط به من الـ `Route Indexer`.
    3. سحب سجلات الخطأ من `app.log` (Laravel) المطابقة.
    4. عرض "الجاني" (Root Cause) للمستخدم أو الوكيل.

---

## 🌐 9. مستشعر المتصفح المتقدم (BrowserCore) 🌐

* **التقنية**: `browser_core.py` المبني على `Playwright`.
* **سير العمل (Lifecycle Flow)**:
    1. بدء تشغيل `Chromium` في وضعية `headless`.
    2. فتح صفحة جديدة وحقن الـ Hooks الحركية.
    3. مراقبة الـ `Network` و الـ `Console` أثناء التنقل.
    4. جمع الأدلة (Screenshots/HAR) في حالة حدوث استثناء.

---

## 👀 10. الإدراك المكاني والسياقي (Perception Layer) 👀

* **الموقع التقني**: `perception.py`.
* **سير العمل (Lifecycle Flow)**:
    1. استلام طلب الفحص لعنصر محدد.
    2. تنفيذ JavaScript داخل الصفحة لاستخراج الـ Metdata.
    3. فحص الجيران (Siblings) والحاوية (Provider).
    4. تعبئة كائن الـ `Context` وإرجاعه لطبقة القرار.

---

## 🖱️ 11. المحرك العضلي (Motor - Physical Physics) 🖱️

* **الموقع التقني**: `motor.py`.
* **سير العمل (Lifecycle Flow)**:
    1. حساب نقطة البداية والنهاية.
    2. توليد نقاط المسار (Path Points) بناءً على منحنى التسارع.
    3. تنفيذ حركات الماوس مع الـ Jitter العشوائي.
    4. التأكد من الوصول للنقطة الهدف بدقة "بشرية".

---

## ⚖️ 12. السياسة التنفيذية السلوكية (Policy Layer) ⚖️

* **الموقع التقني**: `policy.py`.
* **سير العمل (Lifecycle Flow)**:
    1. استلام "النية" (Intent) مثل "Click".
    2. استدعاء الـ Motor للوصول للعنصر.
    3. فحص حالة العنصر (Enabled/Visible) عبر Perception.
    4. تنفيذ الفعل ومراقبة الـ DOM للتأكد من حدوث تغيير.

---

## 🧪 13. التشغيل التجريبي والظل (Shadow & Sandbox) 🧪

* **المنظومة التقنية**: `sandbox.py`.
* **سير العمل (Lifecycle Flow)**:
    1. إنشاء مجلد مؤقت فريد.
    2. استعمال الـ `Git Clone` لنقل الكود.
    3. ربط الـ `vendor` عبر Junction.
    4. تنفيذ الاختبارات، ثم استخراج الـ `diff` لتطبيقه في الخارج.

---

## 🔒 14. حوكمة النطاق (Scope Guardrails) 🔒

* **الموقع التقني**: `guardrails.py`.
* **سير العمل (Lifecycle Flow)**:
    1. استلام قائمة الملفات المراد تعديلها.
    2. مطابقة المسارات مع الـ `Allowlist`.
    3. حساب عدد الأسطر الإجمالية.
    4. إصدار قرار `ALLOW` أو `DENY` مع توضيح السبب.

---

## ⏱️ 15. حوكمة الأداء (Metrics Guard) ⏱️

* **الموقع التقني**: `metrics_guard.py`.
* **سير العمل (Lifecycle Flow)**:
    1. استلام ملف الـ `summary.json`.
    2. مقارنة زمن الـ `move_to_click` بالحدود القصوى.
    3. مقارنة زمن الـ `click_to_dom` بالحدود الدنيا.
    4. إنهاء العملية بـ `Exit 1` في حالة التجاوز.

---

## 🛡️ 16. حوكمة النقاء والنزاهة (Abstraction Guard) 🛡️

* **الموقع التقني**: `check_mouse_layer.py`.
* **سير العمل (Lifecycle Flow)**:
    1. مسح كافة ملفات مجلد `brain`.
    2. البحث عن الأنماط المحظورة (مثل `page.click`).
    3. استثناء الملفات المسموح لها (Motor/Policy).
    4. التقرير عن الانتهاكات في حالة اكتشافها.

---

## 🕵️ 17. نظام التدقيق النشط (BGLGuardian) 🕵️

* **المكون التقني**: `guardian.py`.
* **سير العمل (Lifecycle Flow)**:
    1. بدأ عملية الفحص (Audit Trigger).
    2. تشغيل المسح التكيفي (Adaptive Scan).
    3. تنفيذ سيناريوهات Playwright للتحقق الاستباقي.
    4. إنشاء تقرير التوصيات النهائي.

---

## 🏛️ 18. الامتثال والسيادة المعمارية (BGLGovernor) 🏛️

* **الموقع التقني**: `governor.py`.
* **سير العمل (Lifecycle Flow)**:
    1. قراءة ملفات القواعد (`domain_rules.yml`).
    2. فحص الرموز البرمجية المستخرجة من الـ Indexer.
    3. رصد المكالمات المحظورة (Cross-layer calls).
    4. تصنيف المخالفة (WARN/BLOCK).

---

## 🌉 19. جسر المنطق الحي (Logic Bridge) 🌉

* **التقنية**: `logic_bridge.php`.
* **سير العمل (Lifecycle Flow)**:
    1. فتح قناة `stdin` من البايثون.
    2. تمرير البيانات المهيكلة بالـ JSON.
    3. استدعاء دوال الـ Laravel في الذاكرة.
    4. إرجاع النتيجة المصغرة عبر القنوات المعيارية.

---

## 🎯 20. إدارة الثقة، النوايا، والسياسة (Decisions & Intents) 🎯

* **المحرك التقني**: `decision_engine.py`.
* **سير العمل (Lifecycle Flow)**:
    1. استلام نتيجة التشخيص والعروض المقترحة.
    2. حساب معامل المخاطرة الإجمالي.
    3. تحديد وضعية التنفيذ (Auto/Approval).
    4. تسجيل القرار في جدول الـ `decisions`.

---

## 🖥️ 21. مركز التحكم والقيادة (Agent Dashboard - Command Center) 🖥️

* **الموقع الهيكلي**: `agent-dashboard.php` و المجلد `agentfrontend/`.
* **الوظيفة الاستراتيجية**: يمثل الواجهة البشرية الوحيدة (Human-in-the-loop) التي تسمح للمطور بمراقبة "وعي" الوكيل والتدخل في القرارات المصيرية.
* **المميزات التشغيلية**:
  * **مراقبة البيانات الحية**: عرض حي لمحتويات `knowledge.db` (Blockers, Proposals, Intents, Outcomes).
  * **فك الحظر اليدوي (Blocker Resolution)**: إمكانية التأكيد اليدوي على حل المشكلات التي تعذر على الوكيل حلها ذاتياً.
  * **اعتماد الدستور (Rule Commitment)**: زر "Commit" الذي يحول مقترحات الـ LLM إلى قواعد نافذة في ملف `domain_rules.yml`.
  * **لوحة المؤشرات الحيوية (Vitals)**: مراقبة حالة اتصال قاعدة البيانات، صحة الذاكرة، وحالة المتصفح (Playwright).
* **سير العمل (Lifecycle Flow)**:
    1. قراءة الحالة الراهنة من قواعد البيانات المصغرة (SQLite).
    2. معالجة طلبات الـ POST (مثل الـ Digest أو Force Apply).
    3. التفاعل مع سكريبتات البايثون (مثل `commit_rule.py`) لتنفيذ الأوامر التقنية.
    4. تحديث الواجهة برسائل نجاح أو فشل مهيكلة.

---

## 📉 22. تحليلات الأداء والتحسين التلقائي (Trace Analysis & Auto-Patch) 📉

* **الموقع الهيكلي**: `analysis/analyze_trace.py`.
* **الوظيفة الاستراتيجية**: هذا هو "محرك التحسين" (Optimization Engine) الذي يحلل سجلات الحركة والأداء لاكتشاف الاختناقات البرمجية واقتراح حلول تقنية فورية.
* **القدرات التحليلية**:
  * **تحليل التفاعل (Interaction Analytics)**: حساب زمن الـ `Move to Click` والـ `Click to DOM` لكل عنصر، مما يساعد في رصد العناصر "الثقيلة" في الواجهة.
  * **رصد الاستعلامات البطيئة (Slow Query Detection)**: مسح سجلات الـ `traces.jsonl` لاكتشاف استعلامات SQL التي تتجاوز الـ 100ms.
  * **التصحيح التلقائي (Auto-SQL Patching)**: يمتلك المحرك ذكاءً كافياً لتوليد سكريبتات SQL (Indexes) تلقائياً لتحسين أداء قاعدة البيانات بناءً على الأنماط المكتشفة.
* **سير العمل (Lifecycle Flow)**:
    1. جمع البيانات من `runtime_events` وسجلات الـ Traces.
    2. حساب الإحصائيات (Median, P90) لكل نوع من الأحداث.
    3. تحديد الاستعلامات التي تحتاج إلى "فهرسة" (Indexing).
    4. توليد ملف `analysis/index_patch.sql` لتسريع النظام.

---

## 🛠️ 23. الفحوصات الهيكلية المتخصصة (Modular Structural Guards) 🛠️

* **الموقع الهيكلي**: المجلد `.bgl_core/brain/checks/`.
* **الوظيفة الاستراتيجية**: يضم هذا المجلد مجموعة من "المستشعرات الدقيقة" (Micro-sensors) البرمجية التي تتحقق من صحة ونقاء الكود والبيانات بشكل منفصل ومجزء.
* **أنواع الفحوصات**:
  * **JS/CSS Bloat**: رصد ملفات التنسيق والبرمجة الضخمة التي قد تبطئ واجهة المستخدم.
  * **DB Index Missing**: التأكد من وجود "الفهارس" اللازمة لاستقرار أداء البحث والتقارير.
  * **DB FK Integrity**: فحص وجود مفاتيح الربط (Foreign Keys) لضمان نزاهة العلاقات بين الجداول.
* **سير العمل (Lifecycle Flow)**:
    1. استدعاء الفحص عبر الـ `InferenceEngine`.
    2. قراءة بيانات الـ `inventory.json` أو الـ `schema.json`.
    3. إصدار تقرير `PASS/FAIL` مع الأدلة (Evidence).
    4. تزويد نظام الـ `Guardian` بالبيانات لاتخاذ الإجراءات التصحيحية.

---

## 📖 24. ميثاق الضمان التقني (Technical Assurance Manual) 📖

* **الموقع الهيكلي**: `.bgl_core/logs/BGL3_Technical_Assurance_Manual.md`.
* **الوظيفة الاستراتيجية**: هذا هو "الميثاق" الذي يحدد العقد التنفيذي بين الوكيل والمشروع، ويضمن أعلى معايير الموثوقية (Reliability).
* **ركائز الميثاق**:
  * **عقد التنفيذ الذري (Atomic Integrity)**: ضمان أن كل تغيير برمجي هو "كل أو لا شيء"؛ حيث يتم التراجع (Rollback) الكامل في حالة أي خطأ في الـ Sandbox.
  * **القيود الحتمية (Deterministic Constraints)**: الوكيل يعمل بناءً على "بنية الكود" (AST) والسياسة البرمجية (`domain_rules.yml`) وليس بناءً على التخمين.
  * **التفوق الأدائي (Benchmarks)**: توثيق سرعة استجابة الوكيل التي تصل إلى 21 ضعف سرعة المبرمج البشري مع دقة 100% في التنفيذ المنضبط.
* **الأهمية**: يعتبر هذا المستند هو "شهادة الثقة" التي تؤكد أن الوكيل جاهز للعمل في بيئات الإنتاج الحساسة بأمان مطلق.

---

## 🛠️ 25. التحقق الذاتي من البيئة (Environment Self-Verification) 🛠️

* **الموقع الهيكلي**: `.bgl_core/debug_tools/` و `requirements_report.txt`.
* **الوظيفة الاستراتيجية**: يمتلك الوكيل قدرة داخلية على فحص "صحة البيئة" التي يعمل بها، وضمان توفر كافة المكتبات والاعتمادات (Dependencies) اللازمة لتشغيله بأمان.
* **أدوات الفحص الداخلي**:
  * **Dependency Check**: سكريبتات مثل `check_deps.py` تقوم بمطابقة المكتبات المحملة مع قاعدة المعرفة (`knowledge.db`) لضمان عدم وجود نقص تقني.
  * **Requirements Snapshot**: ملف `requirements_report.txt` الذي يمثل البصمة التقنية (Fingerprint) للمكتبات الأساسية (مثل Jinja2) التي يعتمد عليها الوكيل في توليد التقارير.
  * **KPI Debugging**: أدوات قياس الأداء الداخلي (Debug tools) التي تتأكد من أن سرعة الاستجابة ومنطق التوجيه (Routing) يعملان ضمن المعايير المحددة.
* **الأهمية**: تضمن هذه الطبقة أن الوكيل لن يحاول تنفيذ مهام معقدة في بيئة "غير مكتملة"، مما يقلل من فرص حدوث أخطاء Runtime غير متوقعة.

---

## 🚦 27. وضع فكّ الاختناق المؤقت (Light / Bypass Mode) 🚦

* **المفاتيح البيئية**: `BGL_LIGHT_MODE=1` يحوّل `agent_mode` وقرار التنفيذ إلى `auto`، `BGL_GOVERNOR_BYPASS=1` يتجاوز قواعد `governor.py` بدون حذفها، `BGL_METRICS_GUARD_BYPASS=1` يتخطى `metrics_guard`.
* **الاستخدام**: يُفعّل أثناء الضبط أو الانسداد الميداني، ثم يُزال لإعادة الحوكمة الكاملة.
* **التغطية**: لا يغيّر الملفات الدائمة؛ هو تجاوز في وقت التشغيل فقط.

---

## 🔍 26. تحليل التماسك والازدواجية (Architectural Critical Analysis) 🔍

* **الغرض**: رصد التحديات الهيكلية والنقاط التي قد تؤدي إلى ازدواجية الجهد أو ضعف الاتصال بين المكونات.
* **نقاط الازدواجية (Duplications)**:
  * **تعدد قواعد البيانات**: (تم دمجه) أصبحت الأحداث والقرارات في `knowledge.db` بدلاً من توزيعها بين أكثر من قاعدة.
  * **تشتت أدوات الفحص**: تتوزع سكريبتات الفحص بين `checks/` و `debug_tools/` و `analysis/`؛ مما يجعل من الصعب أحياناً تحديد "المصدر الحقيقي الوحيد" (Single Source of Truth) للصحة التقنية.
* **نقاط التضاد (Conflicts)**:
  * **إرهاق البوابات (Gate Fatigue)**: يعمل كل من `SafetyNet` و `BGLGovernor` كبوابات حماية، ولكن قد تختلف معايير "الفشل" بينهما، مما قد يؤدي إلى رفض التعديلات في طبقة وقبولها في طبقة أخرى إذا لم يتم توحيد سياسات الـ `YAML`.
* **نقاط التفكك (Disintegration Points)**:
  * **هشاشة الجسر البرمجي (PHP-Python Bridge)**: الاعتماد على `stdout` لنقل الـ JSON بين لغتين يمثل مخاطرة؛ فإذا أخرجت لغة PHP خطأً غير متوقع (Fatal Error) خارج نطاق الـ JSON، قد يؤدي ذلك إلى تعطل الـ `Guardian` أو تجاهل الخطأ بصمت.
* **خارطة الطريق للتقوية**:
  * توحيد كافة أدوات الفحص تحت واجهة CLI واحدة.
  * تقوية الجسر البرمجي عبر استخدام بروتوكولات اتصال أكثر متانة.
  * دمج الطبقات الرصدية والتعاملية في واجهة موحدة عبر الـ Dashboard.

---

## 🏁 الخاتمة والهوية الفنية

وكيل BGL3 هو الكيان الأكثر تطوراً في بيئة الإنتاج المحلية؛ فهو يجمع بين الاستشعار الدقيق، التفكير الاستراتيجي عبر Llama 3.1، والتنفيذ الفيزيائي الآمن والمحكوم بأكثر من 20 طبقة حماية متداخلة. المشروع مصمم ليكون "نظاماً يصحح نفسه بنفسه" (Self-Healing System).

> **التوجه الاستراتيجي**: الوكيل الآن في حالة "الاستقلالية الكاملة المنضبطة"، حيث يمتلك القدرة على رصد المعوقات، طلب الأذونات، وتصحيح المسارات المعمارية ذاتياً تحت رقابة نظام الـ Orchestration.

---
