name: goal_route_recent
kind: ui
steps:
- action: goto
  url: http://localhost:8000/
- action: click
  selector: a[href*='get-record.php']
- action: wait
  ms: 800
- action: click
  selector: input[name="search"]
  optional: true
- action: type
  selector: input[name="search"]
  text: bank
  optional: true
  auto_submit: true
- action: press
  selector: input[name="search"]
  key: Enter
  optional: true
