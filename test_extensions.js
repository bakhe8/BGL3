// JavaScript Extension Test
//
// TEST EditorConfig: Indentation should be 2 spaces for JS files
// TEST Copilot: Should suggest code

/**
 * Test function
 * @param {number} a 
 * @param {number} b 
 * @returns {number}
 */
function testExtensions(a, b) {
  // TEST: Type console. and wait for autocomplete
  
  // TEST Copilot: Write a comment like "calculate sum"
  // calculate the sum of a and b
  
  return 0;
}

// TEST GitLens: Hover over this line to see git blame
const result = testExtensions(5, 10);
console.log(result);
