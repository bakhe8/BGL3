name: goal_decision_focus
kind: ui
steps:
- action: goto
  url: http://localhost:8000/
- action: click
  selector: a[href*='agent.php']
- action: wait
  ms: 800
- action: hover
  selector: text=The system has detected multiple
  optional: true
- action: scroll
  dy: 500
- action: click
  selector: input[name="search"]
  optional: true
- action: type
  selector: input[name="search"]
  text: batch
  optional: true
  auto_submit: true
- action: press
  selector: input[name="search"]
  key: Enter
  optional: true
