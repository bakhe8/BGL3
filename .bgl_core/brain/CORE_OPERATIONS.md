# 🧠 BGL3 Agent: CORE OPERATIONS MANUAL 🛡️💎

هذا الملف هو المرجع التقني الشامل والنهائي لمعمارية وسلوك وكيل BGL3 الذكي.

## 1. المعمارية الفنية (Technical Architecture)

يعمل الوكيل بنظام **نواة الذكاء المركزي (AgencyCore)**، وهو كائن مدرك للسيرورة (State-Aware) يتحكم في:

- **المستشعرات (Sensors)**: Playwright (للواجهة) و Laravel Logs (للخلفية).
- **المنطق (Logic)**: ربط الأعطال بالبرمجيات عبر Knowledge Graph.
- **التدبير (Cognition)**: محرك الاستدلال (Inference Engine) الذي يستنتج أنماط الفشل.

## 2. بروتوكول الأمان (Security & Governance)

أي عملية تقع تحت تصنيف "عالية الخطورة" (مثال: تحديث التبعيات عبر Composer) تخضع لبروتوكول **CommanderApproval**:

- يتم تسجيل الطلب في قاعدة البيانات.
- ينتظر الوكيل إشارة `GRANTED` من لوحة التحكم قبل التنفيذ.
- يتم تسجيل كافة العمليات في سجل النشاط (Activity Log) لضمان الشفافية.

## 3. الشفافية والتشخيص (Explained AI)

تم تصميم الوكيل ليكون "مفسراً"؛ عند اقتراح أي قاعدة، يقدم الوكيل:

1. **الأثر المترتب (Impact)**: لماذا يجب رصد هذا النمط؟
2. **الحل المقترح (Solution)**: الخطة البرمجية للمواجهة.
3. **النتيجة المتوقعة (Expectation)**: الفائدة الملموسة.

## 4. المسارات الحيوية (Vital Paths)

- **لوحة القيادة**: `agent-dashboard.php`
- **دستور المشروع**: `.bgl_core/brain/domain_rules.yml`
- **محرك التحقق**: `.bgl_core/brain/master_verify.py`

---

## 🏁 خاتمة التقرير

هذا المجلد يحتوي على "دماغ" الوكيل. تعديل الملفات البرمجية هنا (مثل indexer.py أو inference.py) سيغير مباشرة من قدرات الإدراك والاستنباط لدى الوكيل.

**حالة النظام: Go-Live Ready 🏁**
