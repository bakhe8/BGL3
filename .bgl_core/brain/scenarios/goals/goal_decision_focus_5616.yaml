name: goal_decision_focus
kind: ui
steps:
- action: goto
  url: http://localhost:8000/
- action: click
  selector: input[type='search'], input[name*='search'], input[placeholder*='بحث'],
    input[placeholder*='search']
  optional: true
- action: type
  selector: input[type='search'], input[name*='search'], input[placeholder*='بحث'],
    input[placeholder*='search']
  text: The system is experiencing frequent UI semantic changes, which indicates potential
    issues with the user interface architecture or design. Evolving the architecture
    to improve consistency and maintainability would help address this problem.
  optional: true
- action: press
  selector: input[type='search'], input[name*='search'], input[placeholder*='بحث'],
    input[placeholder*='search']
  key: Enter
  optional: true
- action: wait
  ms: 800
- action: scroll
  dy: 600
- action: click
  selector: input[name="search"]
  optional: true
- action: type
  selector: input[name="search"]
  text: دفعة
  optional: true
  auto_submit: true
- action: press
  selector: input[name="search"]
  key: Enter
  optional: true
