name: goal_decision_focus
kind: ui
steps:
- action: goto
  url: http://localhost:8000/
- action: wait
  ms: 800
- action: hover
  selector: text=The system is expe ie ci g f equ
  optional: true
- action: scroll
  dy: 500
- action: click
  selector: input[name="search"]
  optional: true
- action: type
  selector: input[name="search"]
  text: supplier
  optional: true
  auto_submit: true
- action: press
  selector: input[name="search"]
  key: Enter
  optional: true
