# 🔍 تقرير تدقيق التسمية (Naming Compliance Report)
**الحالة العامة:** ⚠️ **80% متوافق** (توجد مخالفات هيكلية قديمة)

بناءً على "سياسة التسمية الرسمية" الجديدة، قمت بفحص الكود ووجدت المخالفات التالية:

---

## 1️⃣ مخالفات الكلاسات (Classes) - قاعدة 6.3
| الكلاس الحالي | المخالفة | الاقتراح المصحح | درجة الخطورة |
|---|---|---|---|
| `TimelineHelper` | استخدام لاحقة `Helper` (ممنوع) | `TimelineRecorder` أو `TimelineService` | 🔴 **عالية** (منتشر) |
| `TimelineMachine` | اسم غير معياري (`Machine`) | `TimelineController` | 🟡 متوسطة |

---

## 2️⃣ مخالفات الملفات (Files) - قاعدة 7.2
| الملف الحالي | المخالفة | الاقتراح المصحح |
|---|---|---|
| `lib/TimelineHelper.php` | اسم `Helper` + مجلد `lib` (غير قياسي) | `app/Services/TimelineRecorder.php` |
| `public/js/modal-handlers.js` | استخدام لاحقة `handlers` | `public/js/input-modals.controller.js` |
| `public/js/timeline-handler.js` | استخدام لاحقة `handler` | `public/js/timeline.controller.js` |
| `public/js/app_bgl3.js` | اسم غير واضح (`bgl3`) | `public/js/main.js` |

---

## 3️⃣ مخالفات الدوال (Functions) - قاعدة 4.4
| الدالة | المخالفة | الاقتراح المصحح |
|---|---|---|
| `handleManualEntry` | الفعل `handle` (غير محدد) | `submitManualEntry` |
| `handlePasteData` | الفعل `handle` (غير محدد) | `parsePasteData` |

---

## 4️⃣ مخالفات المتغيرات (Variables) - قاعدة 5.2
| المتغير | الموقع | المخالفة | الاقتراح |
|---|---|---|---|
| `$result` | `api/reduce.php`, `api/extend.php` | اسم عام جدًا | `$reductionOutcome`, `$extensionData` |

---

## 📋 التوصية (Action Plan)

لتحقيق الالتزام 100%، يجب إجراء **Refactoring** شامل (إعادة هيكلة) للأسماء أعلاه.
⚠️ **تنبيه:** تغيير اسم `TimelineHelper` سيتطلب تعديل 5 ملفات على الأقل.

**هل تريد البدء في تصحيح الأسماء الآن؟** (أرشح البدء بـ `TimelineHelper` لأنه الأكثر استخدامًا).
