name: goal_gap_deepen
kind: ui
steps:
- action: goto
  url: http://localhost:8000/
- action: wait
  ms: 900
- action: click
  selector: input[type='search'], input[name*='search'], input[placeholder*='بحث'],
    input[placeholder*='search']
  optional: true
- action: type
  selector: input[type='search'], input[name*='search'], input[placeholder*='بحث'],
    input[placeholder*='search']
  text: supplier
  optional: true
- action: press
  selector: input[type='search'], input[name*='search'], input[placeholder*='بحث'],
    input[placeholder*='search']
  key: Enter
  optional: true
- action: wait
  ms: 800
- action: scroll
  dy: 700
- action: click
  selector: '[role=''tab'']'
  optional: true
- action: click
  selector: button.tab-btn, .tab-btn
  optional: true
- action: click
  selector: '[onclick*="switchTab"]'
  optional: true
- action: click
  selector: '[data-filter]'
  optional: true
- action: click
  selector: button.filter, .filter button, .filters button
  optional: true
- action: scroll
  dy: 600
- action: click
  selector: input[name="search"]
  optional: true
- action: type
  selector: input[name="search"]
  text: risk
  optional: true
  auto_submit: true
- action: press
  selector: input[name="search"]
  key: Enter
  optional: true
